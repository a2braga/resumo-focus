# Python
__pycache__/
.pytest_cache/
*.pyc
.env

# PDFs ficam em data/ — binários grandes, não versionar
data/*.pdf

# Textos extraídos são commitados para a Routine ler via GitHub MCP
# data/*.txt  ← versionado de propósito

# output/focus/ é versionado de propósito: contém os resumos gerados pelo agente
